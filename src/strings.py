
def to_upper(text):
     return str(text).upper()


def to_lower(text):
     return str(text).lower()

def capetalize(text):
    return str(text).capitalize()

def ends_with(text, sub_text):
     return str(text).endswith(sub_text)
